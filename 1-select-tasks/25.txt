create table DirtyRead(
Id INT primary key not null,
Value INT not NULL
);
insert into dirtyread values(1,10);

BEGIN;
UPDATE DirtyRead
SET value = value * 10
WHERE id = 1;
SELECT value
FROM DIRTYREAD, PG_SLEEP(5)
WHERE ID = 1;
ROLLBACK;

SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;
BEGIN;
SELECT value
FROM DirtyRead
WHERE id = 1;
