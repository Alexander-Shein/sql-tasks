Create a query to return:

| CategoryName | TotalNumberOfProducts |

USE Northwind
;
SELECT category.CategoryName, SUM(product.UnitsInStock) AS TotalNubmerOfProducts
FROM Categories category
JOIN Products product ON category.CategoryID = product.CategoryID
GROUP BY category.CategoryName