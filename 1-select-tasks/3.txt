Create a query to return all customers from USA without Fax:

| CustomerId | CompanyName |
__________

SELECT c.CustomerId, c.CompanyName
FROM Northwind.public.Customers AS c
WHERE c.Country = 'USA' 
AND Fax IS null;
