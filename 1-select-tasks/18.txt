Create a SQL query to calcualte total orders count by each day in 1998:

| Order Date | Total Number of Orders |

SELECT OrderDate, COUNT(*) FROM dbo.Orders WHERE OrderDate BETWEEN '1998-01-01' AND '1998-12-31' GROUP BY OrderDate;
