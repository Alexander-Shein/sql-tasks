Create a SQL query to calcualte total orders count by each day in 1998:

| Order Date | Total Number of Orders |

USE Northwind
;
SELECT OrderDate, COUNT(OrderID) AS TotalNumberOfOrders
FROM Orders
WHERE YEAR(OrderDate) = YEAR('1998')
GROUP BY OrderDate
