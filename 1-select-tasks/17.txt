Create SQL query to display the average price of each categories's products:

| CategoryName | AvgPrice |

SELECT CategoryName, AVG(UnitPrice) FROM Categories c LEFT JOIN Products p ON c.CategoryID=p.CategoryID GROUP BY CategoryName;
