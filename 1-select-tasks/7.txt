Create a query to return all employees and full name of person to whom this employee reports to:

| EmployeeId | FullName | ReportsTo |

Full Name - title of courtesy with full name.
Order data by first column.
Reports To - Full name. If the employee does not report to anybody leave "-" in the column.


USE Northwind
;

SELECT empl1.EmployeeID, (empl1.FirstName + ' ' + empl1.LastName) AS FullName, IIF(empl1.ReportsTo IS NULL, '-', empl2.FirstName + ' ' + empl2.LastName) AS ReportsTo
FROM Employees empl1
LEFT JOIN Employees empl2 ON empl1.ReportsTo = empl2.EmployeeID
